# import './legislative_session_fragment.graphql'

query GetLegislativeSession($input: LegislativeSessionInput!) {
    legislativeSession(input: $input) {
        ...LegislativeSessionFragment
    }
}