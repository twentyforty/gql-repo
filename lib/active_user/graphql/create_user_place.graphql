# import './active_user_fragment.graphql'

mutation CreateUserPlace($input: CreateUserPlaceInput!) {
    createUserPlace(input: $input) {
        user {
            ...ActiveUserFragment
        }
    }
}
