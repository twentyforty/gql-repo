# import '../../common/graphql/related_image_fragment.graphql'

mutation ProposeRelatedImage($input: ProposeRelatedImageInput!) {
    proposeRelatedImage(input: $input) {
        relatedImage {
            ...RelatedImageFragment
        }
    }
}
