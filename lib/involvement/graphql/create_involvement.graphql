# import './involvement_fragment.graphql'

mutation CreateInvolvement($input: CreateInvolvementInput!) {
    createInvolvement(input: $input) {
        involvement {
            ...InvolvementFragment
        }
    }
}
