
fragment GovernmentWebsiteRenderFragment on GovernmentWebsiteType {
    id
    title
    domain
    url
    faviconUrl
    searchUrlPattern
    approved
    rejected
    canDeleteProposalEntry
    proposalId
    divisionId
}